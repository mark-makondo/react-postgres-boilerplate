{
	"name": "test-app",
	"version": "1.0.0",
	"description": "<div align=\"center\" id=\"top\">    <!-- <a href=\"https://testapp.netlify.app\">Demo</a> --> </div>",
	"main": "index.js",
	"scripts": {
		"build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install && npm run build)",
		"server": "node server/server.js",
		"client": "cd client && npm run start"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"if-env": "^1.0.4"
	}
}
